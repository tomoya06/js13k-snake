{
  "name": "js13k-snake",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "buildgh": "tsc && vite build --mode=github",
    "preview": "vite preview",
    "ghpages": "pnpm run buildgh && gh-pages -d dist"
  },
  "devDependencies": {
    "gh-pages": "^4.0.0",
    "typescript": "^4.6.4",
    "vite": "^3.1.0"
  },
  "dependencies": {
    "kontra": "^8.0.0"
  }
}
